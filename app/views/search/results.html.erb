<div class="max-w-4xl mx-auto">
  <%= render "shared/navigation" %>

  <div class="mt-8">
    <%= link_to "&larr; Back to search".html_safe, search_path, class: "text-indigo-600 hover:text-indigo-800 text-sm" %>

    <div class="mt-4">
      <div class="flex items-center justify-between mb-4">
        <h1 class="text-xl font-semibold text-gray-900">
          Search results for "<%= @query %>"
        </h1>
        <span class="text-sm text-gray-500">
          <%= @topics.count %> results
        </span>
      </div>

      <% if @topics.any? %>
        <div class="space-y-4">
          <% @topics.each do |topic| %>
            <%= render "topics/topic_card", topic: topic, show_favorite: true %>
          <% end %>
        </div>
      <% else %>
        <div class="text-center py-12 bg-white rounded-lg shadow">
          <svg class="w-12 h-12 text-gray-300 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <p class="text-gray-500">No results found for "<%= @query %>"</p>
          <p class="text-gray-400 text-sm mt-1">Try different keywords or check your spelling.</p>
        </div>
      <% end %>
    </div>
  </div>
</div>
